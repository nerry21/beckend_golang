package handlers

import legacy "backend/handlers"

var (
	GetPassengers   = legacy.GetPassengers
	CreatePassenger = legacy.CreatePassenger
	UpdatePassenger = legacy.UpdatePassenger
	DeletePassenger = legacy.DeletePassenger
)
